DirectoryIndex index.html

RewriteEngine On

# Handle all requests that don't exist as files or directories
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d  
RewriteRule ^(.*)$ index.html [L]